# Core Dependencies
streamlit>=1.24.0,<2.0
pandas>=2.0.0,<3.0
numpy>=1.26.0,<3.0

# Data Visualization
plotly>=5.15.0,<6.0
altair>=5.0.0,<6.0
bokeh>=3.2.0,<4.0

# Security & Authentication
python-jose>=3.3.0,<4.0
passlib>=1.7.4,<2.0
python-multipart>=0.0.6,<0.1
bcrypt>=4.0.1,<5.0

# Cloud Integration
boto3>=1.28.0,<2.0  # AWS
azure-identity>=1.13.0,<2.0  # Azure
azure-mgmt-resource>=23.0.0,<24.0

# Monitoring & Metrics
# elasticsearch/opensearch-py are unused by dashboard/src today (no code
# imports them) but track the Elastic Stack 9.x / OpenSearch 2.x floors
# documented in README.md for when that integration is built.
prometheus-client>=0.17.0,<1.0
elasticsearch>=9.0.0,<10.0
opensearch-py>=2.3.0,<3.0

# Development, testing, linting, and documentation tooling lives in
# dashboard/requirements-dev.txt (not installed in production images).
