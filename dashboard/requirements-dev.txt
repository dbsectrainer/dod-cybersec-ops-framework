# Classification: UNCLASSIFIED // FOR OFFICIAL USE ONLY (FOUO)
# Development, testing, linting, and documentation tooling.
# Install alongside the root requirements.txt: pip install -r ../requirements.txt -r requirements-dev.txt
-r ../requirements.txt

# Testing
pytest>=8.0.0,<9.0
pytest-cov>=5.0.0,<6.0

# Lint & format (ruff replaces black + flake8)
ruff>=0.6.0,<1.0

# Type checking
mypy>=1.10.0,<2.0
types-PyYAML>=6.0.12,<7.0

# Documentation
sphinx>=7.3.0,<8.0
sphinx-rtd-theme>=2.0.0,<3.0
